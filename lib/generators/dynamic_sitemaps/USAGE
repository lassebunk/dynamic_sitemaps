Description:
    Generates initializer and route for dynamic_sitemaps.

Example:
    rails generate dynamic_sitemaps sitemap.xml

    This will create:
        config/initializers/sitemap.rb
        Route "match 'sitemap.xml' => 'sitemaps#sitemap'"
